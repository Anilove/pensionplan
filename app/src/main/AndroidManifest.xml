<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.pensionplan.pensionplan">

    <application
        android:name=".PensionPlan"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUp" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".loginPage" />
        <activity android:name=".TrusteesListShow" />
        <activity android:name=".AnsDepositReq" />
        <activity android:name=".AnsLumpsumReq" />
        <activity android:name=".AnsLumpsumWith" />
        <activity android:name=".AnsMonthlyWith"></activity>
    </application>

</manifest>